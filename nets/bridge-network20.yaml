apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-network20
spec:
  config: '{
    "cniVersion": "0.3.0",
    "name": "bridge-network20",
    "type": "bridge",
    "bridge": "br20",
    "ipam": {
      "type": "whereabouts",
      "range": "192.168.20.0/24",
      "network_name": "bridge-network20",
      "enable_overlapping_range": false
    }
  }'
