apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-network10
spec:
  config: '{
    "cniVersion": "0.3.0",
    "name": "bridge-network10",
    "type": "bridge",
    "bridge": "br10",
    "ipam": {
      "type": "whereabouts",
      "range": "192.168.10.0/24",
      "network_name": "bridge-network10",
      "enable_overlapping_range": false
    }
  }'
